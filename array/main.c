#include <stdio.h>
#include <float.h>
int main () {
   printf("max value of float = %.10e\n", FLT_MAX);
   printf("min value of float = %.10e\n", FLT_MIN);
   printf("max value of double = %.10e\n", DBL_MAX);
   printf("min value of double  = %.10e\n", DBL_MIN);

}
